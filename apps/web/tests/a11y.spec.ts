/**
 * verify:a11y — audit d'accessibilité (SETUP_PLAN.md, phase 9).
 *
 * axe-core sur chaque section, tabulation complète, focus visible, ARIA des
 * onglets, contraste. Échec bloquant.
 *
 * Squelette volontairement vide (test.skip) tant que la page n'existe pas :
 * il n'y a rien à auditer avant les phases 5-8. À remplir en phase 9.
 */

import { test } from '@playwright/test';

test.skip('axe-core sur chaque section — à écrire en phase 9', () => {});
test.skip('tabulation complète et focus visible — à écrire en phase 9', () => {});
test.skip('ARIA des onglets de la section interactive — à écrire en phase 9', () => {});
