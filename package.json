{
  "name": "cadance",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "turbo run build",
    "mobile": "pnpm --filter @cadance/mobile start",
    "android": "pnpm --filter @cadance/mobile android",
    "ios": "pnpm --filter @cadance/mobile ios",
    "api": "pnpm --filter @cadance/api dev",
    "web": "pnpm --filter @cadance/web dev",
    "db:migrate": "pnpm --filter @cadance/api db:migrate",
    "db:generate": "pnpm --filter @cadance/api db:generate",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "~5.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
