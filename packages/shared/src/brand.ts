/**
 * Le nom de marque, en un seul endroit.
 *
 * Toute surface (app mobile, landing page, PDF de facture) doit importer
 * cette constante — jamais ecrire le nom en dur. Cote web, il est expose
 * en variable CSS `--brand-name` generee depuis cette constante.
 */
export const BRAND_NAME = 'Cadance';
